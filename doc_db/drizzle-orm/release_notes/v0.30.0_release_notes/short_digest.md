**Breaking Change:** `postgres.js` driver now always returns date strings; Drizzle maps them based on `mode` setting. Timestamps use `.toISOString()` conversion. Multiple timestamp/date bugs fixed across different scenarios and timezone handling.