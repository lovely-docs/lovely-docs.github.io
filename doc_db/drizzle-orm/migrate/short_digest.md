Applies SQL migrations generated by `drizzle-kit generate`. Reads migration files, checks database log, runs unapplied migrations, and records them in `__drizzle_migrations` table.

Configuration via `drizzle.config.ts` or CLI with `dialect` and database credentials. Supports multiple config files for different database stages.