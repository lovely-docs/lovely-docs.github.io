**50+ headless Svelte components** with compound architecture, flexible state management (two-way or getter/setter binding), keyboard nav, ARIA accessibility, data-attribute/CSS-variable styling. **State**: `bind:value={state}` or `bind:value={getter, setter}`. **Styling**: CSS frameworks, data attributes (`[data-state]`, `[data-disabled]`), global/scoped CSS, CSS variables (`--bits-[component]-*`). **Child snippet**: Override rendering via `{#snippet child({ props })}` with `{...props}` spread; floating components use two-level `wrapperProps`/`props` structure. **Ref prop**: `bind:ref={element}` for DOM access. **Transitions**: `forceMount` + `child` snippet with Svelte transitions. **Date/Time**: Immutable `DateValue` types (CalendarDate, CalendarDateTime, ZonedDateTime) from `@internationalized/date` with `.set()`, `.add()`, `.subtract()` methods; 1-indexed months. **Utilities**: `BitsConfig` (global defaults), `mergeProps`, `Portal`, `useId`, `IsUsingKeyboard`. **Type helpers**: `WithElementRef`, `WithoutChild`, `WithoutChildren`, `WithoutChildrenOrChild`. **v0→v1 breaking changes**: `el`→`ref`, `asChild`→`child` snippet, `multiple`→`type` prop, `selected`→`value`, auto-portalling→explicit `Portal`, `Tooltip.Provider` required.