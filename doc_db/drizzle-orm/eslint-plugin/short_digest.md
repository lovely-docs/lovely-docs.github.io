ESLint plugin with two rules: `enforce-delete-with-where` and `enforce-update-with-where` to prevent accidental deletion/update of all rows. Both support optional `drizzleObjectName` config to target specific object names and avoid false positives from non-Drizzle delete/update methods.