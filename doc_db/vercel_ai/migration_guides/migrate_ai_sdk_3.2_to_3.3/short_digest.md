**No breaking changes**. New features: OpenTelemetry support, `useObject` hook for streaming structured data, `useChat` improvements (attachments, streaming tool calls, `onFinish` callback), raw JSON schema support, AWS Bedrock provider, enhanced LangChain adapter with `toDataStreamResponse`, UI framework expansions (SolidJS feature parity, Vue.js `useAssistant`, Svelte tool calling).