Navigation API with lifecycle hooks (`afterNavigate`, `beforeNavigate`, `onNavigate`), programmatic navigation (`goto`), data/code preloading (`preloadData`, `preloadCode`), load function invalidation (`invalidate`, `invalidateAll`, `refreshAll`), scroll control (`disableScrollHandling`), and shallow routing (`pushState`, `replaceState`).