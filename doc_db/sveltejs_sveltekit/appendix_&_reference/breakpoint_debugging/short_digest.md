**VSCode**: Use Debug Terminal (`Cmd/Ctrl+Shift+P` → "Debug: JavaScript Debug Terminal") or set up `.vscode/launch.json` with `"type": "node-terminal"` and use Run and Debug pane.

**Browser DevTools**: Run `NODE_OPTIONS="--inspect" npm run dev`, open site, click Node.js logo in dev tools, or navigate to `chrome://inspect` / `edge://inspect`.

**Other editors**: WebStorm and Neovim have community debugging guides.