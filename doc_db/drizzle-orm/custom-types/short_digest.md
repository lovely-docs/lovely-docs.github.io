Create custom database types using `customType()` with generic type parameters for `data`, `driverData`, and `config`. Implement `dataType()` to return SQL type string, optionally add `toDriver()` and `fromDriver()` for value mapping. Examples for Postgres (serial, text, boolean, jsonb, timestamp) and MySQL (int, text, boolean, json, timestamp) with usage in table definitions.