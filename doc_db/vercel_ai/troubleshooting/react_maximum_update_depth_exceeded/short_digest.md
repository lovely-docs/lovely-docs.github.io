When streaming AI responses with `useChat` or `useCompletion`, use `experimental_throttle: 50` option to throttle UI updates and prevent "Maximum update depth exceeded" errors caused by re-rendering on every chunk.