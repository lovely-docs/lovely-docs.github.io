`{@debug ...}` logs variable values on change and pauses execution with devtools open. Accepts comma-separated variable names only: `{@debug user}` or `{@debug user1, user2, user3}`. Use `{@debug}` alone to trigger on any state change.