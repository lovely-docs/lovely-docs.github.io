**LibSQL batch API**: Execute multiple queries in one call with `db.batch([...])`. **SQLite JSON mode**: `text('col', { mode: 'json' }).$type<T>()`. **Relational Query `.toSQL()`**: Get SQL from query builders. **PostgreSQL array operators**: `arrayContains()`, `arrayContained()`, `arrayOverlaps()`. **Relational Query operators**: Import operators from where callback `(table, { inArray })` instead of separately. **Fixes**: MySQL datetime UTC mapping, SQLite on-conflict where, RDS raw SQL, smallserial type.