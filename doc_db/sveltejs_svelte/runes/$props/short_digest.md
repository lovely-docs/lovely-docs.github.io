`$props` rune receives component inputs with destructuring, fallback values, renaming, and rest properties. Props update reactively but shouldn't be mutated unless bindable. Add type annotations for safety. `$props.id()` generates unique instance IDs for element linking.