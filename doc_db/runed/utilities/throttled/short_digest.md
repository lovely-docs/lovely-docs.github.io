Throttled state wrapper that delays updates by a specified interval. Constructor takes a getter function and delay in ms. Access throttled value via `.current`. Methods: `cancel()` to discard pending updates, `setImmediately(value)` to update immediately.