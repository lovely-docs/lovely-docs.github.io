## TiDB Integration in Existing Project

Install `@tidbcloud/serverless`, set `DATABASE_URL` env var, configure Drizzle with `mysql` dialect, introspect existing database, connect and query using `tidb-serverless` dialect. Optionally update schema and apply changes.