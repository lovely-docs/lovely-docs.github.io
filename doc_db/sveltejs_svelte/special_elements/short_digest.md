**svelte:boundary** - Error boundaries with pending/failed snippets; catches rendering/effect errors only. **svelte:window/document/body** - Attach listeners and bind properties (scrollX/Y writable, others readonly) with auto-cleanup; top-level only. **svelte:head** - Insert into document.head. **svelte:element** - Dynamic tag from `this` prop; only `bind:this` works. **svelte:options** - Compiler options: runes, namespace, customElement, css injection.