Calendar component for date range selection using @internationalized/date. Bind a `{start, end}` value object and apply classes for styling.

```svelte
<RangeCalendar bind:value class="rounded-md border" />
```

Install: `npx shadcn-svelte@latest add range-calendar -y -o`