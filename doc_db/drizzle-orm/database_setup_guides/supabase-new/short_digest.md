## Setup Drizzle with Supabase

Install postgres package → set DATABASE_URL env var → connect Drizzle to database → define schema → configure drizzle.config.ts (postgresql dialect) → run migrations → seed/query database → run index.ts