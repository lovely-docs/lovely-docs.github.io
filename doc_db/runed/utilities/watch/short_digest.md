Manually specify which reactive values trigger a callback via getter function. Supports single/multiple sources, deep object watching with `$state.snapshot()`, and `lazy` option. Variants: `watch.pre`, `watchOnce`, `watchOnce.pre`.