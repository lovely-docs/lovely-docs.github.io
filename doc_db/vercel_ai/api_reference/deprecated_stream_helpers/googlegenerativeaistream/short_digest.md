**Deprecated** (removed in AI SDK 4.0). Transforms Google Generative AI SDK responses into ReadableStream using AIStream with Google-specific parsing.

**Import**: `import { GoogleGenerativeAIStream } from "ai"`

**Parameters**: `response` (GenerateContentResponse stream), optional `callbacks` (onStart, onToken, onCompletion, onFinal)

**Returns**: ReadableStream

**Note**: Use AI SDK Google Generative AI Provider instead.