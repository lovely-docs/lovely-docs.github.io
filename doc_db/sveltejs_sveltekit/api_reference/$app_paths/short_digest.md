Module for resolving asset URLs and paths. Key functions: `asset(file)` - resolve static asset URLs with base/assets prefix; `resolve(pathname | routeId, params?)` - prefix pathname with base path or populate route ID dynamic segments. Deprecated: `assets`, `base`, `resolveRoute`.