**Text generation**: generateText (non-streaming), streamText (streaming) with tool calling, structured outputs, multi-step generation. **Structured data**: generateObject/streamObject with Zod/JSON schemas, multiple output modes. **Embeddings**: embed (single), embedMany (batch with chunking). **Reranking**: rerank documents by relevance. **Experimental**: generateImage, transcribe, generateSpeech. **Agents**: Agent interface, ToolLoopAgent with stop conditions, UI stream helpers (createAgentUIStream, createAgentUIStreamResponse, pipeAgentUIStreamToResponse). **Tools**: tool() for type inference, dynamicTool() for runtime schemas. **MCP**: createMCPClient with stdio transport. **Schemas**: jsonSchema, zodSchema (with useReferences for recursion), valibotSchema. **Messages**: ModelMessage (system/user/assistant/tool roles with multimodal parts), UIMessage (type-safe UI state with metadata/data/tools). **Validation**: validateUIMessages, safeValidateUIMessages. **Providers**: createProviderRegistry (providerId:modelId), customProvider. **Utilities**: cosineSimilarity, wrapLanguageModel with middleware, extractReasoningMiddleware, simulateStreamingMiddleware, defaultSettingsMiddleware, stop conditions (stepCountIs, hasToolCall), simulateReadableStream, smoothStream (text smoothing with chunking strategies), generateId, createIdGenerator.