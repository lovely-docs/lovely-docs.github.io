**Error**: `Type 'SomeModel' is not assignable to type 'LanguageModelV1'` (or `EmbeddingModelV3`)

**Cause**: Model specification updates create incompatibilities with older provider versions.

**Fix**: Update provider packages and AI SDK to latest versions.