Initialize Gel project, define schema in `dbschema/default.esdl`, run migrations, install `drizzle-orm` and `gel`, configure `drizzle.config.ts` with `dialect: 'gel'`, pull schema with `drizzle-kit pull`, then connect and query using `drizzle({ client: createClient() })` with standard insert/select/update/delete operations.