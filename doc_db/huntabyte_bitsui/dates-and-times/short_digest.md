Bits UI uses `DateValue` objects from `@internationalized/date` for dates/times. Three types: `CalendarDate` (date only), `CalendarDateTime` (date+time), `ZonedDateTime` (date+time+timezone). Create via constructors or parse functions (`parseDate`, `parseDateTime`, `parseZonedDateTime`, `parseAbsolute`, `parseAbsoluteToLocal`). Objects are immutable—update using `.set()`, `.add()`, `.subtract()`, or `.cycle()`. Use `DateFormatter` for locale-aware formatting. Placeholder prop controls initial date and calendar view. Month indexing is 1-based.