**Routing**: Rest `[...file]`, optional `[[lang]]`, matchers, hex/Unicode escapes `[x+nn]`/`[u+nnnn]`, layout groups `(group)`, hierarchy breaking `@segment`. **Hooks**: `handle`, `resolve` (transformPageChunk/filterSerializedResponseHeaders/preload), `handleFetch`, `handleValidationError`, `handleError`, `init`, `reroute`, `transport`. **Errors**: `error(status, msg)` → `+error.svelte`, unexpected → `handleError` hook, customize with `src/error.html` and `App.Error`. **Links**: `data-sveltekit-preload-data/code/reload/replacestate/keepfocus/noscroll`. **Service Workers**: `src/service-worker.js` auto-registered, access `$service-worker` (build/files/version/base), implement install/activate/fetch. **Server-Only**: `.server` suffix or `$lib/server/` prevents browser imports, errors on any import chain. **Snapshots**: `capture()`/`restore(value)` in `+page.svelte`/`+layout.svelte` for ephemeral state. **Shallow Routing**: `pushState`/`replaceState` for modals, `preloadData` for route data, `page.state` access. **Observability**: OpenTelemetry spans via `src/instrumentation.server.ts`, `event.tracing` for custom attributes. **Packaging**: `@sveltejs/package`, `src/lib` public, `package.json` exports with conditions, auto-generated types, fully-specified imports with extensions.