**Server**: `Server` class with `init()`, `respond()`. **Errors**: `error()`, `fail()`, `redirect()`, type guards. **Responses**: `json()`, `text()`, `normalizeUrl()`. **Forms**: `Action`, `ActionFailure`, `ActionResult`. **Adapters**: `Adapter`, `Builder` with file ops, path resolution, build generation, instrumentation. **Navigation**: `Navigation`, `NavigationTarget`, `AfterNavigate`, `BeforeNavigate`, `Page`. **Load**: `Load`, `LoadEvent` with `fetch`, `setHeaders()`, `parent()`, `depends()`, `untrack()`, `tracing`. **Requests**: `RequestEvent`, `RequestHandler`, `Cookies` with get/set/delete. **Hooks**: `Handle`, `HandleClientError`, `HandleServerError`, `HandleFetch`, `HandleValidationError`, `ClientInit`, `ServerInit`, `Reroute`, `Transport`. **Remote**: `RemoteCommand`, `RemoteQuery`, `RemoteForm` with `enhance()`, `validate()`, `fields`. **Validation**: `Invalid` for imperative errors. **Misc**: `Snapshot`, `ParamMatcher`, `Emulator`, `ResolveOptions`, `Prerendered`, `RouteDefinition`, `SSRManifest`, `HttpError`, `Redirect`. **Helpers**: `sequence()` for middleware chaining. **Node.js**: `createReadableStream()`, `getRequest()`, `setResponse()`, `installPolyfills()`. **Vite**: `sveltekit()` plugin. **Modules**: `$app/environment` (browser, building, dev, version), `$app/paths` (asset, resolve), `$app/forms` (applyAction, deserialize, enhance), `$app/navigation` (afterNavigate, beforeNavigate, goto, invalidate, preloadData, pushState), `$app/server` (command, form, query, prerender, read), `$app/state` (navigating, page, updated), `$app/types` (RouteId, Pathname, RouteParams), `$env/*` (dynamic/static, public/private), `$lib`, `$service-worker`. **Config**: adapter, alias, appDir, csp, csrf, embedded, env, experimental (tracing, instrumentation, remoteFunctions), files, inlineStyleThreshold, moduleExtensions, outDir, output (preloadStrategy, bundleStrategy), paths, prerender, router (type, resolution), typescript, version. **Types**: Generated `$types` per route, `app.d.ts` with `App.*` interfaces. **CLI**: vite dev/build/preview, svelte-kit sync.