**Core concepts**: Generative models, LLMs (with hallucination limitations), embeddings. **Providers**: 50+ unified interface (OpenAI, Anthropic, Google, Mistral, etc.) with capability matrix. **Prompts**: Text/system/message types; multi-modal content (text, images, files); provider options at function/message/part levels. **Tools**: LLM-callable objects with description, inputSchema (Zod/JSON), execute function; ready-made packages (Composio 250+, agentic 20+, etc.) and MCP servers. **Streaming**: Use `streamText` with `for await` to display response parts incrementally instead of blocking.