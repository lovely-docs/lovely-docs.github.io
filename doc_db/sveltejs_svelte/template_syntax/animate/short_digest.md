Animations trigger when keyed each block contents reorder. Custom animation functions receive `node`, `{ from, to }` DOMRects, and `params`, returning `{ delay, duration, easing, css?, tick? }`. Use `css(t, u)` for web animations or `tick(t, u)` for imperative updates; prefer `css` for performance.