Generates a unique identifier string with optional custom length (defaults to 16). Uses the same generator as the AI SDK internally.

```ts
import { generateId } from 'ai';
const id = generateId();
```