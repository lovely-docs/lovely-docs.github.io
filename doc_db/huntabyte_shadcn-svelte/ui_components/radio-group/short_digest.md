## Radio Group

Mutually exclusive selection component. Install with `npx shadcn-svelte@latest add radio-group -y -o`.

Basic usage wraps `RadioGroup.Root` with `RadioGroup.Item` elements paired with labels. Supports form integration via sveltekit-superforms with validation and error handling through the Form component.