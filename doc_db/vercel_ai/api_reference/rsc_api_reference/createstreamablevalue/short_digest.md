Creates a server-to-client stream for serializable data via `createStreamableValue(value)`. Returns a streamable object returnable from Server Actions with an update method. (Experimental; use AI SDK UI for production.)