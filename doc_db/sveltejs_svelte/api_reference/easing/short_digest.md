32 easing functions for animations: `linear`, quadratic/cubic/quartic/quintic/sine/expo/circular/back/bounce/elastic variants with `In`/`Out`/`InOut` modifiers. Each takes normalized time `t` and returns eased value.

```js
import { cubicIn, bounceOut } from 'svelte/easing';
```