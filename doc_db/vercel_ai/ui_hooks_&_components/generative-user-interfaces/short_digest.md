Generative UI connects LLM tool calls to React components. Use `useChat` hook for chat interface, define tools with `createTool` (with description, inputSchema, execute function), create React components matching tool output shape, then render components by checking `message.parts` for tool-specific parts with type `tool-${toolName}` and handling their states (input-available, output-available, output-error).